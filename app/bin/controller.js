// Generated by CoffeeScript 1.6.2
(function() {
  var ModelView,
    __bind = function(fn, me){ return function(){ return fn.apply(me, arguments); }; };

  ModelView = (function() {
    function ModelView() {
      this.load = __bind(this.load, this);
      this.log_json = __bind(this.log_json, this);
      var _this = this;

      this.outer = ko.observable(new App.group({
        form_name: "outer_group"
      }));
      this.file_name = ko.observable();
      this.file = ko.computed(function() {
        return "data:application/json," + (ko.toJSON(_this.outer));
      });
    }

    ModelView.prototype.log_json = function() {
      return console.log(ko.toJSON(this.outer, null, '\t'));
    };

    ModelView.prototype.load = function() {
      var config;

      config = JSON.parse(window.prompt("Please enter the config info to load."));
      return this.outer(new App.group(config));
    };

    return ModelView;

  })();

  $(document).ready(function() {
    var modelview;

    modelview = new ModelView();
    return ko.applyBindings(modelview);
  });

}).call(this);

/*
//@ sourceMappingURL=controller.map
*/
